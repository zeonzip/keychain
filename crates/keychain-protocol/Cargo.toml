[package]
name = "keychain-protocol"
version = "0.1.0"
edition = "2024"

[dependencies]
bytes = "1.12.1"
interprocess = { version = "2.4.2", features = ["tokio"] }
thiserror = "2.0.19"
tokio = { version = "1.53.1", optional = true, features = ["io-util"] }

[features]
tokio = ["dep:tokio"]
